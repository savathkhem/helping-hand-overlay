export function init() { return Promise.resolve(); }
export function listRecentCaptures(limit = 10) { return Promise.resolve([]); }
export function saveCapture(capture) { return Promise.resolve(capture); }

